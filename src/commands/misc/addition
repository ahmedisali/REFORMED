const { SlashCommandBuilder, Client } = require('discord.js');

module.exports = {
 data: new SlashCommandBuilder().setName('Add').setDescription('Adds the Numbers'),
  run: ({ interaction }) => {
    interaction.reply(`Command Still In Dev Here Is The Bots Uptime ${client.uptime}`);
}
}
